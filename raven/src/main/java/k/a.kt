package k

import com.solar.tabor.ScGoImpl

/**
 * Date：2025/10/16
 * Describe:
 * k.a
 */
class a {

    // 10个混淆风格的无用变量
    private val zx = 9
    private val q2 = "x"
    private val rT = listOf(1, 2)
    private val p9 = 3.14f
    private val s7 = true
    private val tY = LongArray(5)
    private val uK = Any()
    private val v3 = StringBuilder()
    private val wQ = setOf("a", "b")
    private val eP = mutableMapOf<Int, String>()

    fun a(int: Int): Any {
        if (int == zx) return ScGoImpl() else return 90
    }

    // 3个混淆风格的无用方法
    private fun b7(x: Int): String {
        return x.toString() + q2
    }

    private fun cR(): Boolean {
        return s7 && zx > 0
    }

    private fun d5(list: List<Int>): Int {
        var sum = 0
        list.forEach { sum += it }
        return sum + zx
    }

    companion object {

        @JvmStatic
        fun o(string: String, str2: String) {

        }
    }

}